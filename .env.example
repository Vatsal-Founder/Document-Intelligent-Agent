OPENAI_API_KEY=

# optional — only needed if you re-run notebook/ingest_docs.py against gated
# HuggingFace models; the embedding model used at runtime is public
HUGGINGFACEHUB_API_TOKEN=

# optional — LangSmith tracing, off by default
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=
LANGSMITH_TRACING=
